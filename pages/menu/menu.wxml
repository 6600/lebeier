<!-- 音乐播放模板 -->
<import src ="../template/template.wxml"/>
<template wx:if="{{showLogin}}" is="login" />
<view wx:else class="content">
  <template is="switch"  data="{{flag, style}}"/>
  <view class='title-bar'>
    <navigator url="../Music/Music" open-type="reLaunch">音乐</navigator>
    <view wx:for="{{navigation}}" wx:key="index">
      <text>></text>
      <text wx:key="index">{{item}}</text>
    </view>
  </view>
  <view class='player-box'>
    <view class='list-one' wx:for="{{cardList}}" wx:key="{{index}}" data-id="{{item.id}}" data-name="{{item.name}}" catchtap="getMusicPage" >
      <span data-id="{{item.id}}" data-name="{{item.name}}">{{item.name}}</span>
    </view>
  </view>
</view>

<!-- 播放器 -->
<template is="Playback" data="{{isPlaying, totalProcess, sliderValue, player, currentTime, totalTime, musicName, style, isListLoop}}"/>